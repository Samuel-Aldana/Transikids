import 'package:flutter/material.dart';

class Imgoogle extends StatelessWidget {
  const Imgoogle({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      "assets/images/google-logo.jpg",
    );
  }
}